{
  "name": "quiz",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:js": "babel src -d dist",
    "build:css": "sass ./assets/styles/style.scss dist/style.css",
    "dev:js": "babel -w src -d dist",
    "dev:css": "sass --watch ./assets/styles/style.scss dist/style.css",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.7.0",
    "@babel/core": "^7.7.0",
    "@babel/preset-env": "^7.7.1",
    "jasmine": "^3.5.0",
    "sass": "^1.23.3"
  }
}
